<table class="table table-bordered table-striped hidden-mobile" style="width: 95%">
        <tbody>
           <td colspan="4"><h4>Código: {{entity.curso.nombre}} Nombre: {{ entity.curso.nombre }} </h4></td>
            <tr>
                <th>Programa</th>
                <td>{{ entity.curso.programa? entity.curso.programa.nombre : 'Sin' }} - {{ entity.curso.escuela }}</td>
            </tr>
            <tr>
                <td colspan="2"><b>Nivel:</b> {{ entity.curso.nivel }} <b>Tipologia:</b> {{ entity.curso.tipologia }} <b>Creditos:</b> {{ entity.curso.creditos }}</td>
            </tr>
        </tbody>
    </table>
    <b>Tutores Oferta: {{entity.periodo}} </b>
        <table class="table table-bordered table-striped hidden-mobile" style="width: 95%">
        <tbody>
          <tr>
              <th>ID</th>
                <th>Nombre</th>
                <th>Centro</th>
                <th>Borrar</th>
            </tr>
        {%for tutor in entity.tutores %}
            <tr>
                <td>{{tutor.docente.id}}</td>
                <td>{{tutor.docente.user.id}} - {{tutor.docente.user.nombres}} {{tutor.docente.user.apellidos}}</td>
                <td>{{tutor.docente.centro.nombre}}</td>
               <td> <a href="{{ path('tutor_delete', {'id': tutor.id }) }}" class="btn btn-success"> Borrar</a>
                </td>
            </tr>
        {%endfor%}
        </tbody>
        </table>
    
<div class="modal fade" data-refresh="true" id="ModalBorrar" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
<div class="modal-dialog modal-dialog">
<div class="modal-content">
                <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times</button>
                <h4 class="modal-title">Eliminar Tutor de Oferta</h4>
                </div>			<!-- /modal-header -->
                <div class="modal-body">
                <ul>
                    <li>
                    IMPORTANTE: Elimine el docente unicamente si este no fue tutor virtual en este curso y para este periodo. 
                    </li> 
                </ul>
                </div>
                <div class="modal-footer">
                    <form method="get" action="/">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Borrar</button>
                    </form>
                </div>
</div></div>
</div>   