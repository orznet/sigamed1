{% block body -%}
<body>
<link rel="stylesheet" type="text/css" href="{{ asset('css/print.css') }}">
    <h3>Evaluación de la red de Tutores (Heteroevaluación)</h3>
 
    <h3>1. Evaluaciones recibidas como Director:</h3>
{%set final=0%} {%set aux=0%}
<table>
{%for curso in cursos%}    
        <thead>
           <tr>
           <th colspan="3"><h4>{{curso.id}}-{{curso.nombre}} (Tutores: {%set cont = 0%}{%for tutor in curso.evaldetutor%} {% set cont = cont + 1%} {%endfor%}{{cont}})</h4></th>
            </tr>
            <tr>
                <th>Tutor</th>
                <th>Observaciones</th>
                <th>Calificación</th>
            </tr>
        </thead>
        {%if cont > 0%}
            <tbody>    
            {%for tutor in curso.evaldetutor%}
            {%if tutor.f0%}{%set final=final+tutor.f0%}{%set aux=aux+1%}{%endif%}        
            <tr>
                            <td>{{tutor.tutor.user.nombres}} {{tutor.tutor.user.apellidos}}</td>
                            <td>{{tutor.observaciones}}</td>
                            <td>{{tutor.f0? tutor.f0 : 'Sin'}}</td>
            </tr>
            {%endfor%}

{%endif%}
{%endfor%}
            <tr>
                <td colspan="2" align="right"><b>Calificación Final:</b></td>
                <td>{%if final > 0%}{%set final = final/aux%} {{final|number_format(2, '.', ',')}}{%endif%}</td>
            </tr>
            </tbody>
</table>

   <h3>2. Cursos que evaluo</h3>
<table>
        <thead>
            <tr>
                <th>Curso</th>
                <th>Nombre</th>
                <th>Director</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for evaluacion in entity.getEvaladirector() %}
            <tr>
                <td>{{evaluacion.curso.id}}</td>
                <td>{{evaluacion.curso.nombre}}</td>
                <td>{{evaluacion.curso.director.user.nombres}} {{evaluacion.curso.director.user.apellidos}}</td>
                <td>
                {%if evaluacion.fecha %}
                Evaluado
                {%else%}
                Sin evaluar
                 {%endif%}
                 </td>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}