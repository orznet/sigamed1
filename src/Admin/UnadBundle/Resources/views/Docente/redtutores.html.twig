
{% extends '::base.html.twig' %}
{% block body -%}
<h4><a href="{{ path('docente_show', {'id': app.session.get('docenteid') }) }}"> << Regresar</a>
    <h1>Evaluación de la red de Tutores (Heteroevaluación)</h1>
    <h1>1. Cursos para evaluar</h1>
    <table class="table table-bordered table-striped hidden-mobile" style="width: 95%">
        <thead>
            <tr>
                <th>Curso</th>
                <th>Nombre</th>
                <th>Director</th>
                <th>Evaluar</th>
            </tr>
        </thead>
        <tbody>
        {% for evaluacion in entity.getEvaladirector() %}
            <tr>
                <td>{{evaluacion.curso.id}}</td>
                <td>{{evaluacion.curso.nombre}}</td>
                <td>{{evaluacion.curso.director.user.nombres}} {{evaluacion.curso.director.user.apellidos}}</td>
                <td>
                {%if evaluacion.fecha %}
                Evaluado ({{evaluacion.f0}})
                {%else%}
                <a href="{{ path('redtutores_edit', { 'id': evaluacion.id }) }}">Evaluar...</a>
                 {%endif%}
                 </td>
        {% endfor %}
        </tbody>
    </table>
    <h1>2. Cursos a cargo como director:</h1>
{%set final=0%} {%set aux=0%}
{%for curso in cursos%}            
<h4>{{curso.id}}-{{curso.nombre}} (Tutores: {%set cont = 0%}{%for tutor in curso.evaldetutor%} {% set cont = cont + 1%} {%endfor%}{{cont}})</h4>    
{%if cont > 0%}
<table class="table table-bordered table-striped hidden-mobile" style="width: 95%">
        <thead>
            <tr>
                <th>Tutor</th>
                <th>Correo</th>
                <th>Evaluo?</th>
            </tr>
        </thead>
            <tbody>    
            {%for tutor in curso.evaldetutor%}
            {%if tutor.f0%}{%set final=final+tutor.f0%}{%set aux=aux+1%}{%endif%}        
            <tr>
                            <td>{{tutor.tutor.user.nombres}} {{tutor.tutor.user.apellidos}}</td>
                            <td>{{tutor.tutor.user.email}}</td>
                            <td>{{tutor.f0? 'Si' : 'No'}}</td>
            </tr>
            {%endfor%}
            </tbody>
</table>
{%endif%}
{%endfor%}
<table class="table table-bordered table-striped hidden-mobile" style="width: 95%">
    <tr>
        <td>Calificación Final</td> 
        <td>{%if final > 0%}{%set final = final/aux%} {{final}}{%endif%}</td>
    </tr>    
</table>
<p>Apreciado docente: solicite a sus tutores evaluar su curso para poder obtener una calificación final de este instrumento</p>
{% endblock %}