{% extends '::base.html.twig' %}
{% block body -%}
    <h3>Roles Asignación Académica de:  {{ entity.id.user.nombres}} {{ entity.id.user.apellidos}} : {{ entity.id.user.id}}</h3>

     <table class="table table-bordered table-striped hidden-mobile" style="width: 95%">
        <tbody>
            <tr>
                <td>Fecha creación: {{ entity.fechacreacion? entity.fechacreacion|date('Y-m-d H') :'' }}</td>
                <td>Fecha cierre: {{ entity.fechacierre? entity.fechacierre|date('Y-m-d H:i') : '' }}</td>
            </tr>
            <tr>
                <td colspan="2">Estado: 
                {{entity.estado}}    
                </td>
            </tr>
            <tr>
              <td colspan="2">
              <h5> Apreciado Docente:</h5>
<ul>
<li>Para realizar su autoevaluación primero debe confirmar su asignación académica.</li>
<li>La asignación académica es el registro de los roles únicamente y esta información es suministrada por la oficina de Talento Humano.</li>
<li>La autoevaluación se realiza en base los roles de formación e investigación únicamente.</li>
<li>El aplicativo asigna las actividades de cada rol automáticamente y estas no se pueden modificar.</li>
<li>La autoevaluación para los roles de formación no se realiza para cada curso o periodo especifico se realiza para el desempeño en general. </li>
<li>Si considera que con la información registrada no se puede autoevaluar por favor envié un correo a la dirección (siga@unad.edu.co) y especifique que rol no corresponde o cual hace falta.</li>
</ul>
                  
                </td>   
            </tr>
        </tbody>
    </table>  
    
 </table>
  <table class="table table-bordered table-striped hidden-mobile" style="width: 95%">
        <thead>
            <tr>
                <th>Rol</th>
            </tr>
        </thead>
        <tbody>
      {% set num = 0 %} {% set totalhoras = 0 %}
      {% set termino = 1 %}
      {% for rol in entity.roles %}
   
            <tr>
                {% set num = num+1 %}
                <td>{{num}}.     
                {{ rol.rol.nombre }}<br>
                {%if rol.rol.textoampliacion%}
                {{rol.descripcion}}
                {%endif%}
                </td>
               </tr>
        {% endfor %}

               <tr>
                <td colspan="3">
               {%if entity.estado != 'Confirmado' %}
                <a data-toggle="modal" data-target="#ModalCerrar" class="btn btn-labeled btn-success"><span class="btn-label"><i class="glyphicon glyphicon-thumbs-up"></i></span>Confirmar Asignación</a>
               {%else%}
                 <a href="{{ path('plangestion_show', { 'id': entity.id.id }) }}" class="btn btn-primary btn-xs">Autoevaluación<i class="fa fa-sign-in"></i></a>
                {%endif%}
                   </td>
               </tr>
       
        </tbody>
    </table>
 <br>
 <br>

 <div class="modal fade" data-refresh="true" id="ModalCerrar" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
<div class="modal-dialog modal-sm">
<div class="modal-content">
                <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times</button>
                <h4 class="modal-title">Confirmar Asignación</h4>
                </div>			<!-- /modal-header -->
                <div class="modal-body">
                IMPORTANTE: 
Confirme su asignación para poder autoevaluarse, luego de confirmar no podrá solicitar ajustes a la asignación en caso que realmente se requieran.<br>
                </div>
                <div class="modal-footer">
               <form  action="{{ path('plangestion_confirm', { 'id': entity.id.id }) }}" method="get">     
                <button type="button" class="btn btn-default" data-dismiss="modal">
                        Cancelar
                </button>
               <td><button type="hidden" class="btn btn-primary">Confirmar Asignación</button></td>    
               </form> 
                </div>
</div></div>
     
<div class="modal fade" data-refresh="true" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
<div class="modal-header modal-sm">
<div class="modal-content">
</div></div></div>
    
{% endblock %}
