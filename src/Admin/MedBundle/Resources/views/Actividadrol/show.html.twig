{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Actividadrol</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Texto_ampliacion</th>
                <td>{{ entity.textoampliacion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('actividadrol') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('actividadrol_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
