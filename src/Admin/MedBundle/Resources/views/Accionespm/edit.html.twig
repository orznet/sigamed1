{% block body -%}
               <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times</button>
            <h4 class="modal-title">Editar Item</h4>
            </div>			<!-- /modal-header -->
            <div class="modal-body">          
   <form  action="{{ path('accionespm_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
   <table class="table table-bordered table-striped hidden-mobile">
   <input type="hidden" name="_method" value="PUT" />
     {{ form_errors(edit_form) }}
     <div style="display:none;">
      {{ form_widget(edit_form.plan) }}
     </div>
   <tr>
       <td>Oportunidad de Mejora</td>
       <td>{{ form_widget(edit_form.oportunidad) }}</td>
   </tr>
      <tr>
       <td>Acción Propuesta</td>
       <td>{{ form_widget(edit_form.accion) }}</td>
   </tr>
   <tr>
    <td></td>
    <td><button type="hidden" class="btn btn-lg btn-primary">Guardar</button></td>
  </tr>
  {{ form_widget(edit_form._token) }}
  </table>
</form>
{{ form(delete_form) }}                
{% endblock %}
