{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar Centro</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('zona') }}">
            Regresar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
