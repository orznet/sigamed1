{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Escuela creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('escuela') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
